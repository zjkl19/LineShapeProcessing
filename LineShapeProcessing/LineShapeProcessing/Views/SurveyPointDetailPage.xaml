<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="LineShapeProcessing.Views.SurveyPointDetailPage"
             Title="{Binding Title}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="删除" Clicked="Delete_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Text:" FontSize="Medium" />
            <Label Text="{Binding SurveyPoint.No}" d:Text="SurveyPoint No" FontSize="Small"/>
            <Label Text="Description:" FontSize="Medium" />
            <Label Text="{Binding SurveyPoint.BacksightPoint}" d:Text="SurveyPoint BacksightPoint" FontSize="Small"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>